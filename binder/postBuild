# install atomistics
pip install .

# copy notebooks
mv notebooks/*.ipynb .
cp -r tests/static .

# clean up
rm -rf .ci_support .github atomistics atomistics.egg-info binder docs notebooks tests .coveralls.yml .gitattributes .gitignore .readthedocs.yml CITATION.cff CODE_OF_CONDUCT.md LICENSE MANIFEST.in README.md pyproject.toml setup.py build
